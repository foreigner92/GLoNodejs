'use strict';

angular.module('imvgm')
  .controller('EmailVerificationCtrl', ['$scope', '$rootScope', '$dialog', '$route', '$location', function($scope, $rootScope, $dialog, $route, $location) {
    console.log("WTF?");
    console.log($route);

  }]);
